{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "npm run build && npm run dev",
    "dev": "lerna run dev",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
